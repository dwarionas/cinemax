query GetSlicedData($searchQuery: String!, $page: Int!) {
    getSliced(searchQuery: $searchQuery, page: $page) {
        title
        name
        id
        poster_path
        media_type
        release_date
        first_air_date
    }
}