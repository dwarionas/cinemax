mutation CreateUser($input: auth) {
    createUser(input: $input) {
        emailError
        passwordError
        id
        password
        email
        role
        bookmarks {
            title
            name
            poster_path
            first_air_date
            release_date
            id
            bookmarkID
        }
        joined
    }
}